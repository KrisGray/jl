const amanda = require('./amanda.js');
const tyler = require('./tyler.js');
const debug = require('debug')('main');


console.log(amanda.geometricSum(1, 2, 5));
console.log(amanda.quadraticFormula(1, 2, -15));
console.log(tyler.sphereVolume(2));

debug("starting");
